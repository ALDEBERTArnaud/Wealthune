import 'package:flutter/material.dart';

/// Classe de couleurs personnalisées pour l'application.
class AppColors {
  // Couleur principale utilisée dans l'application.
  static const Color primaryColor = Color.fromARGB(255, 236, 187, 88);

  // Couleur secondaire utilisée pour les arrière-plans ou les accents.
  static const Color secondaryColor = Color(0xFF0b1c2c);

  // Couleur utilisée pour indiquer les erreurs ou les suppressions.
  static const Color errorColor = Color.fromARGB(255, 231, 51, 19);

  // Couleur utilisée pour indiquer les confirmations ou les succès.
  static const Color confirmationColor = Color.fromARGB(255, 123, 224, 137);
}
